{
  "name": "UserProfile",
  "version": "1.0.0",
  "description": "Display user profiles browser and get their JSON via API calls",
  "main": "app/server.js",
  "proxy": "http://localhost:8080/",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node app/server.js"
  },
  "author": "Zeynep Dayar",
  "license": "ISC",
  "dependencies": {
    "express": "^4.13.4"
  },
  "engines": {
    "node": "^6.0.0"
  }
}
